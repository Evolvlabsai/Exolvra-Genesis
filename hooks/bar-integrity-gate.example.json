{
  "_comment": "OPT-IN; requires exolvra-genesis on PATH. Before every subagent dispatch, resolve state.json's active run and verify .exolvra-genesis/runs/<run-id>/bar/bar.sha256. Malformed IDs, missing pins and changed artifacts fail closed. Nothing in the plugin depends on this hook.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Task|Agent",
        "hooks": [
          {
            "type": "command",
            "command": "exolvra-genesis gate --phase bar"
          }
        ]
      }
    ]
  }
}
